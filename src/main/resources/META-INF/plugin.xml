<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <id>dev.gaphunter.kafkaproducerreusecompanion</id>
    <name>Kafka Producer Reuse Companion</name>
    <vendor>Gap Hunter Labs</vendor>
    <description><![CDATA[
        <p>Gutter warning icon on a <code>new KafkaProducer&lt;&gt;(...)</code>
        construction written inside a regular method body -- Apache
        Kafka's own javadoc, unchanged across every released version,
        states "The producer is thread safe and sharing a single
        producer instance across threads will generally be faster than
        having multiple instances." Building one inside a method means
        the real connection/initialization cost is paid on every call,
        and defeats internal batching across calls.</p>
        <p><b>100% static text/PSI analysis</b> -- matches the class
        name by simple text, so it works whether the real Kafka client
        jar is on the classpath or not. Java and Kotlin.</p>
        <p>Stated honestly: v0.1 only checks <code>KafkaProducer</code>
        -- <code>KafkaConsumer</code> is deliberately excluded (unlike
        the producer, a single consumer instance is not safe to use
        from multiple threads concurrently, so "build once, reuse"
        isn't the same unqualified recommendation there). Never flags a
        construction inside a constructor or a field initializer.</p>
        <p>Free. No license nags, no telemetry, no network calls.</p>
    ]]></description>

    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.java</depends>
    <depends>org.jetbrains.kotlin</depends>

    <extensions defaultExtensionNs="org.jetbrains.kotlin">
        <supportsKotlinPluginMode supportsK1="true" supportsK2="true"/>
    </extensions>

    <extensions defaultExtensionNs="com.intellij">
        <codeInsight.lineMarkerProvider language="JAVA"
            implementationClass="dev.gaphunter.kafkaproducerreusecompanion.gutter.ProducerBuiltPerCallLineMarkerProvider"/>
        <codeInsight.lineMarkerProvider language="kotlin"
            implementationClass="dev.gaphunter.kafkaproducerreusecompanion.gutter.ProducerBuiltPerCallLineMarkerProvider"/>
        <notificationGroup id="Kafka Producer Reuse Companion" displayType="BALLOON"/>
    </extensions>
</idea-plugin>
